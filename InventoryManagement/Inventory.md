# Inventory Management
## Instruction
1. Using UNIX prompt, Run:
```
sudo service apache2 start
sudo service mysql start
```
2. Then, copy code into `/var/www/html`
```
sudo cp -r /path/to/project /var/www/html
```
3. In your web browser, go to https://localhost/mainmenu.php
